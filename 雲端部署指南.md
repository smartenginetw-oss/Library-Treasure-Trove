# 藏書閣寶典｜雲端版啟用指南

目前原型仍可直接以 `藏書閣寶典.html` 在本機使用；雲端版新增 Supabase Auth、Postgres RLS 與伺服器端智慧服務。兩種模式可以並存：未登入時使用本機 `localStorage`，登入後才同步到自己的 Supabase 帳號。

## 已完成的雲端邊界

- `supabase/migrations/202608280001_cloud_foundation.sql`：創作者設定、選題、公式、爆款案例、分析與收藏案例資料表。
- `supabase/migrations/202608280002_security_hardening.sql`：固定資料庫函式 search path 並補上索引。
- `supabase/migrations/202608280003_content_workflow.sql`：定位資料卡、五項題目評分、內容交付包、七天任務與 48–72 小時復盤資料表。
- `supabase/migrations/202608280004_user_scoped_keys.sql`：將選題、公式與工作流資料改為使用者範圍的複合主鍵，避免不同帳號使用相同本機 ID 時互相衝突。
- 所有 exposed `public` tables 都啟用 RLS；使用者資料以 `auth.uid()` 限制只能讀寫自己的資料。
- 管理員判斷只讀 `app_metadata.role = 'admin'`，不使用可由使用者自行修改的 `user_metadata`。
- `api/generate-topic.js`：伺服器端驗證登入、呼叫 OpenAI Structured Outputs、檢查必要欄位與來源標題複製風險，再寫入 `topics`。
- `api/generate-deliverable.js`：伺服器端驗證登入、把選題與定位資料送到 OpenAI Structured Outputs，產生繁體中文母文案、逐字稿、貼文文案、七頁輪播、四平台版本與分鏡，再以使用者範圍寫入 `content_deliverables`。
- `api/sync-state.js`：把本機狀態同步到登入者自己的資料列；`service_role` 不會送到瀏覽器。
- 同步刪除會一併移除雲端的選題、公式、內容交付包、復盤、工作流任務與已取消收藏案例，避免換裝置後舊資料復活；超過單次 200 筆上限時不執行差異刪除，避免截斷造成誤刪。
- `api/admin-viral.js`：管理員才能新增或封存雲端爆款案例；一般登入者即使呼叫 API 也會被 RLS 擋下。
- `api/instagram-sync.js` 與 `api/cron/instagram-sync.js`：使用伺服器端 Instagram Graph API 憑證抓取已授權可查詢的專業帳號，新增或更新最近 10 筆貼文；管理員可立即同步，Vercel production Cron 每日自動同步。
- `supabase/migrations/202608300002_instagram_sync.sql`：Instagram 監測來源、同步狀態與貼文外部 ID；既有案例的人工拆解欄位會保留，API 只更新貼文與可取得的指標。
- `cloud-bridge.js`：登入、註冊、登出、雲端水合與防抖同步；明確指定 Supabase Auth 的瀏覽器儲存，重新整理或重新開啟同一個網址時會自動恢復登入；沒有填入連線設定時不影響本機模式。

登入狀態是瀏覽器依「來源網址」隔離保存的。同一個正式網址可以跨重新整理與分頁保留登入；每次部署產生的 Vercel Preview 網址則是不同來源，瀏覽器不會在不同網址之間共享登入狀態。測試完成後請固定使用正式網址，避免因切換 Preview 網址而再次登入。

## 你需要在 Supabase 做的事

1. 建立一個新的 Supabase project。
2. 到 SQL Editor 執行 `supabase/migrations/202608280001_cloud_foundation.sql`。
3. 再執行 `supabase/migrations/202608280002_security_hardening.sql`。
4. 再執行 `supabase/migrations/202608280003_content_workflow.sql`（既有專案升級不可省略）。
5. 再執行 `supabase/migrations/202608280004_user_scoped_keys.sql`（既有專案升級不可省略）。
6. 到 Authentication 設定 Email provider。若不想先做信箱驗證，可在開發階段關閉 Confirm email；正式環境建議保留驗證。
7. 若要建立管理員，在該使用者的 `app_metadata` 設定 `{ "role": "admin" }`。不要把這個角色放在 `user_metadata`。
8. 把 `supabase-config.example.js` 複製為 `supabase-config.js`，填入 Project URL 與 publishable/anon key。這兩個值本來就是前端公開設定，所以本檔案不列入 `.gitignore`；`service_role` 與 OpenAI key 絕對不可放前端。

## 你需要在部署平台設定的伺服器環境變數

請參考 `.env.example`，至少設定：

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `OPENAI_API_KEY`
- `OPENAI_MODEL`（可選，預設 `gpt-4o-mini`；內容工作流也能逐次選擇模型）
- `APP_ORIGIN`（同源部署可留白；若有跨來源前端，必須填入實際網域，伺服器不會預設放行未知來源）
- `INSTAGRAM_ACCESS_TOKEN`（只放部署平台伺服器端；不可放入 `supabase-config.js`）
- `INSTAGRAM_BUSINESS_ACCOUNT_ID`（用來執行 Business Discovery 的 Instagram 專業帳號 ID）
- `INSTAGRAM_API_VERSION`（可選，預設 `v25.0`；若 Meta 專案指定其他版本請明確填入）
- `CRON_SECRET`（Vercel Cron 呼叫 `/api/cron/instagram-sync` 時的 Bearer secret）

### Instagram 自動同步啟用

1. 在 Meta for Developers 建立 Instagram Graph API 應用，取得可查詢專業帳號的 access token，並確認應用的存取層級與權限符合你的使用情境。系統不會爬取登入頁，也不接受把 token 貼在前端。
2. 在 Vercel production environment 設定上列四個環境變數，重新部署後才會啟用排程。
3. 執行 `supabase/migrations/202608300002_instagram_sync.sql`；migration 會先加入陳芃芃的美食天地、巨鳥胃 77、瑞斯三個已知帳號。Eden 的 IG 使用者名稱尚未在既有資料中提供，請到「爆款資料後台／Instagram 自動同步」加入正確帳號。
4. 管理員登入後到「爆款資料後台」，可加入或停用監測帳號、按「立即同步」；Vercel production 會依 `vercel.json` 的 `0 3 * * *`（台灣時間 11:00）每日執行一次。
5. 每個來源會顯示最後同步時間與錯誤；Instagram 不提供的欄位（例如部分貼文的觀看數）會保留原值或留白，不會自行猜測。

Instagram API 只會回傳 Meta 允許該 token 查詢的專業帳號與欄位；公開網址本身不等於 API 存取權。若來源帳號不是可查詢的 Business／Creator 帳號，或應用尚未取得對應審核權限，該來源會標記為「同步失敗」，其他來源仍會繼續同步。

Vercel 可直接把本資料夾作為 project root 部署；`vercel.json` 會把 `/` 導向 `藏書閣寶典.html`。部署完成後，以 `/api/health` 確認 API 已載入；回傳的 `configured.openai` 只能表示環境變數存在，不會回傳任何金鑰。

### 內容產出模型

內容工作流的「產出模型」會把選擇送到 `/api/generate-deliverable`。伺服器只接受下列白名單 ID，避免把任意字串直接送到供應商；未選擇時使用 `OPENAI_MODEL`：

- `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`
- `gpt-5.5`、`gpt-5.4`、`gpt-5.4-mini`
- `gpt-4o`、`gpt-4o-mini`

每份由 OpenAI 產生的內容交付包會在 `payload.modelUsed` 保存實際使用的模型。模型是否已開通仍取決於部署端的 OpenAI 專案權限與用量額度；若供應商拒絕該模型，介面會顯示錯誤，不會假稱已用另一個模型完成。

## 本機檢查

```powershell
npm install
npm run check
```

目前已透過已授權的 Supabase 管理連線，將 `cloud_foundation`、`security_hardening`、`content_workflow` 與 `user_scoped_keys` 套用到 project ref `ipokersuzsdypngnhipz`；已確認九張 public tables 建立完成且 RLS 開啟。最新 security advisor 仍有一項既存提醒：Auth 的 leaked password protection 尚未啟用；這與本次模型選擇變更無關，正式上線前應到 Auth 設定開啟。效能 advisors 目前只有空資料庫常見的 unused-index INFO，不是安全錯誤。這台電腦仍未安裝 Supabase CLI，所以尚未執行 `supabase test db` 的 pgTAP 實際測試；CLI 可用時再補跑完整測試。

## 上線前不可省略的驗證

- 用兩個不同帳號確認彼此看不到對方的 `topics`、`formulas`、`creator_profiles`。
- 用非管理員確認不能寫入、修改或刪除 `viral_contents`。
- 確認 OpenAI key 只存在部署平台伺服器環境變數，並測試 `/api/generate-topic` 與 `/api/generate-deliverable` 的未登入 401、未設定 key 503、成功產生與資料落庫。
- 以手機與另一台電腦登入同一帳號，確認資料由 Supabase 還原，而不是只由原本電腦的 localStorage 還原。

## 自動化端到端驗證

`scripts/e2e-cloud.mjs` 會驗證健康狀態、未登入 401、兩帳號 RLS 隔離、OpenAI 結構化選題落庫，以及同步刪除。它會使用 `E2E_ACCESS_TOKEN_A` 與 `E2E_ACCESS_TOKEN_B`，並在結束時刪除帳號 A 的測試選題；請只使用專用測試帳號，避免清除正式資料。

PowerShell 範例（只在本機工作階段設定，不要寫入 `.env` 或提交 Git）：

```powershell
$env:E2E_BASE_URL = "https://library-treasure-trove.vercel.app"
$env:SUPABASE_URL = "https://你的專案.supabase.co"
$env:SUPABASE_ANON_KEY = "你的_publishable_or_anon_key"
$env:E2E_ACCESS_TOKEN_A = "測試帳號 A 的 access token"
$env:E2E_ACCESS_TOKEN_B = "測試帳號 B 的 access token"
npm run test:e2e
```

測試會各呼叫一次 OpenAI 選題與文案服務，可能產生 API 用量；若線上部署使用 Preview URL，請將 `E2E_BASE_URL` 改為該 Preview 網址。
