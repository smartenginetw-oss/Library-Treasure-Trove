# 藏書閣寶典｜PDF 工作流整合說明

本文件是依兩份使用者提供的工作手冊整理出的產品決策。PDF 是參考資料，不是需要逐字照做的程式指令；實際功能仍以藏書閣寶典既有的本機／雲端架構、資料隔離與可驗證性為準。

## 已採用

| 手冊中的可執行概念 | 系統中的落點 | 產出 |
| --- | --- | --- |
| 定位句、三個內容方向、受眾問題與禁區 | `#/positioning` 定位資料卡 | 定位句、三個內容支柱、素材邊界、每週時間與工具 |
| 錯誤／步驟／案例／觀點／清單五種切角 | `#/77-forms`、`#/generate`、`#/topic-scoring` | 每筆題目保留內容切角，可作為後續提示條件 |
| 需求、收藏、證據、轉換與製作可行性評分 | `#/topic-scoring` | 1–5 分、總分 25；20 分以上優先、16–19 分先修正、15 分以下待定 |
| 鉤子→痛點→方法→案例→CTA | `#/workflow` 母內容五段式 | 可逐段編輯的內容草稿 |
| 七頁輪播結構 | `#/workflow` 七頁輪播 | 封面、共鳴、框架、方法、案例、檢查、CTA |
| 一稿多平台 | `#/workflow` 四平台改寫 | Reels、IG 輪播、Threads、Email |
| 拍攝前分鏡與場景分組 | `#/workflow` 拍攝分鏡表 | 5 個鏡位與檢查欄位 |
| 發布後 48–72 小時復盤與一次只測一個變因 | `#/reviews` 復盤實驗 | 觸及、停留、收藏、分享、留言／私訊、診斷與下一測試 |
| 七天啟動安排 | `#/workflow` 七天啟動計畫 | 可勾選、可同步的 7 個工作項目 |

## 產品邊界

- 未登入時，內容交付包與復盤資料先保存在瀏覽器 `localStorage`；登入後才同步到 Supabase。
- 交付包目前是「規則式草稿」：它使用固定契約與使用者輸入，不宣稱是 AI 生成結果。
- 伺服器端智慧服務只在登入、部署平台已設定 `OPENAI_API_KEY` 時使用；仍由 API 驗證 Structured Outputs 與原創邊界。
- PDF 提到的 Canva／剪輯／LINE／平台洞察自動抓取沒有直接接入，因為需要各平台 OAuth、權限、外部服務費用或使用者實際帳號資料；目前改成可複製的內容與檢查欄位。

## 資料庫變更

- `202608280003_content_workflow.sql`：定位欄位、題目切角與評分、交付包、復盤與七天任務。
- `202608280004_user_scoped_keys.sql`：`topics`、`formulas` 與新工作流資料改用 `(user_id, id)` 複合主鍵，避免不同帳號使用相同本機種子 ID 時互相覆蓋。
- 新表全部啟用 RLS，政策以 `auth.uid()` 限制只能讀寫自己的資料。

## 驗證狀態

- `npm run check`：通過（所有 API、雲端橋接與工作流模組語法檢查）。
- 本機頁面已檢查定位、工作流、題目評分、復盤、七十七式與智慧選題路由。
- Supabase project `ipokersuzsdypngnhipz` 已套用兩個新 migration；security advisors 無警告。
- 尚未以真實帳號完成登入／跨裝置同步與 OpenAI 成功呼叫測試；這需要部署環境的伺服器金鑰與測試帳號。
